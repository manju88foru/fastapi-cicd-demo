name: Auto Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy on server
        run: |
          cd /opt/fastapi-cicd-demo
          git pull
          docker compose down
          docker compose up -d --build